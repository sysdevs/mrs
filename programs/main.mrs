func add a b {
    i16 something = 52
    return a * b + something
}

func main {
    i32 x = 5
    str text1 = 'hello world'
    str text2 = 'fizz buzz'

    i8 value = input()

    if (value > x) {
        output(text1)
    } else {
        output(text2)
    }
}
